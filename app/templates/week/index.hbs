<ul class="week-list">
  {{#each day in model.days itemController="week.day"}}
    <li>
      {{#link-to "week.day" day class=day.status}}
        <div class="day-info">
          <div class="day">
            {{#if day.current}}
              Today
            {{else}}
              {{format-date day.date format="dddd"}}
            {{/if}}
          </div>
          <div class="routines">
            {{#if day.totalRoutines}}
              {{day.totalRoutines}} {{pluralize-string day.totalRoutines s="Routine"}}
              {{#if day.totalWorkouts}}
                &middot;
                {{day.totalWorkouts}} {{pluralize-string day.totalWorkouts s="Workout"}}
              {{/if}}
            {{else}}
              Rest Day
            {{/if}}
          </div>
        </div>
        <i class="fa fa-lg fa-angle-right"></i>
      {{/link-to}}
    </li>
  {{/each}}
</ul>
