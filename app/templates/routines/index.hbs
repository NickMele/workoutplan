<div class="flex-row actions">
  {{#if isEditing}}
    <a {{action "edit"}} class="fadein">
      <i class="fa fa-times-circle-o"></i> Cancel
    </a>
  {{else}}
    <a {{action "edit"}}>
      <i class="fa fa-check-circle-o"></i> Edit
    </a>
   {{/if}}
  {{#link-to "routines.new"}}<i class="fa fa-plus"></i> Create{{/link-to}}
</div>
<ul class="routines-list border-top">
  {{#each routine in model itemController="routines.show"}}
    <li>
      <span>
        {{#if parentController.isEditing}}
          <i class="fa fa-lg fa-times-circle fadein red" {{action "delete" routine}}></i>
          {{#link-to "routines.edit" routine}}{{routine.name}}<i class="fa fa-angle-right fadein"></i>{{/link-to}}
        {{else}}
          {{#link-to "routines.show" routine}}{{routine.name}}{{/link-to}}
        {{/if}}
      </span>
    </li>
  {{/each}}
</ul>